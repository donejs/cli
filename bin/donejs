#!/usr/bin/env node

var cli = require('../lib/cli');
var utils = require('../lib/utils');

utils.projectRoot()
  .then(function(root) {
    cli(root, process.argv);
  })
  .catch(function(err) {
    throw err;
  });
